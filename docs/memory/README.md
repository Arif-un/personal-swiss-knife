# docs/memory — lazy-loaded notes

Feature-specific / deep context. NOT loaded every session (unlike `/CLAUDE.md`).
Read a file here only when working on that feature.

When to add a file here vs. `/CLAUDE.md`:
- Every-session fact (convention, global gotcha) → `/CLAUDE.md`.
- Deep, feature-scoped detail (a spec, an architecture decision, a subtle flow) → new file here, then link it below.

## Index

<!-- - [messenger-bubble.md](messenger-bubble.md) — floating chat-head bubble: states, drag/snap, shortcuts -->
_(empty — add entries as notes are written)_
